# Anthropic API key for the build-time enrichment script.
# Used only by scripts/enrich.mjs — never imported into any file in src/.
# Copy this file to .env (gitignored) and fill in your key.
ANTHROPIC_API_KEY=sk-ant-...
