# Story 1.1 — Data layer with first UI render

**Status:** Done
**Estimate:** 105 min
**Depends on:** Story 1.0

## Story

Stand up the data layer behind the `dataStore` interface and prove it works by replacing the Story 1.0 placeholder with a simple unstyled vehicle list. TDD on `validateBid` — the only piece of real business logic in the app. Every downstream story consumes what this story builds.

## Files touched

- `src/lib/constants.ts` — `MIN_BID_INCREMENT`, `MAX_BID`, `AUCTION_DURATION`, `ENDING_SOON_THRESHOLD`, `AWAY_THRESHOLD`
- `src/lib/timestamps.ts` — median-anchor normalization, `getAuctionStatus(vehicle, now)`
- `src/lib/dataStore.ts` — `DataStore` interface + localStorage implementation
- `src/lib/bids.ts` — `validateBid` pure function
- `src/hooks/useNow.ts` — single-clock `useSyncExternalStore`
- `src/hooks/useVehicles.ts`, `src/hooks/useBids.ts`
- `src/pages/LandingPage.tsx` — **update** to render unstyled list of 20 vehicles
- Tests: `timestamps.test.ts`, `bids.test.ts` (TDD), `dataStore.test.ts`

## Acceptance criteria

- **AC1** `dataStore.getVehicles()` returns 200 vehicles with `auction_start` timestamps normalized via median-anchor offset. The JSON file on disk is never mutated.
- **AC2** `getAuctionStatus(vehicle, now)` returns `'upcoming' | 'live' | 'ended'` correctly: upcoming when `now < auction_start`, live when in `[auction_start, auction_start + AUCTION_DURATION]`, ended after.
- **AC3** `validateBid` returns a `BidResult` discriminated union. Rejects amount `<= current_bid`, amount `> MAX_BID`, and any bid when status is not `live`. Accepts amount `>= starting_bid` when `current_bid` is null. Accepts amount `>= current_bid + MIN_BID_INCREMENT` otherwise.
- **AC4** `dataStore.submitBid` persists to localStorage under one key, replays on cold start, and fires subscribers via `useSyncExternalStore`.
- **AC5** `useNow()` ticks once per second and re-renders subscribers via `useSyncExternalStore`. Tests can fake the clock.
- **AC6** `LandingPage.tsx` renders an unstyled `<ul>` of the first 20 vehicles, each `<li>` showing `{year} {make} {model} — {auction_status}`. The status text updates live as the clock ticks across status thresholds.

## Tests (TDD on `validateBid`)

Write these tests first; they all fail. Then implement `validateBid` until they pass.

- `validateBid rejects amount equal to current_bid`
- `validateBid rejects amount below current_bid + MIN_BID_INCREMENT`
- `validateBid rejects amount above MAX_BID`
- `validateBid rejects when auction status is upcoming`
- `validateBid rejects when auction status is ended`
- `validateBid accepts amount equal to starting_bid when current_bid is null`
- `validateBid accepts amount equal to current_bid + MIN_BID_INCREMENT`

Additional tests (after implementation):

- `normalizeTimestamps shifts the median to "now" for an arbitrary dataset`
- `getAuctionStatus returns the correct status across the three windows`
- `dataStore.submitBid persists and replays across a fresh module load`

## How to verify (UI)

1. `npm run dev`
2. Browser opens http://localhost:5173 — see the heading from 1.0 plus an unstyled list of 20 vehicle rows.
3. Each row shows year, make, model, and auction status (Upcoming / Live / Ended). Roughly a third of each status type, balanced around "now".
4. Wait a minute — at least one row's status text should change as auctions transition.
5. `npm run test` — all unit tests pass, including the TDD bid validation suite.

## Commit

```
feat: data layer with normalized timestamps, dataStore, bid validation (TDD)
```

## Watch for / Notes

- The unstyled list is a temporary surface to prove wiring. Story 1.2 replaces it with the styled card grid.
- If the page shows mostly "Ended" statuses, median-anchor normalization is probably off. The median of the unmodified `auction_start` values across the dataset should land at roughly `now` after the offset is applied.
- LocalStorage persists across dev sessions. Use the dev-tools "Application" tab to clear it between test runs if bids stack up.
- `useNow()` is the single clock for the whole app. No other component should own a `setInterval`.

## Story complete when

- Every AC above is true on `main`.
- All TDD tests are green.
- `npm run verify` exits zero in CI.
- Status above updated to "Done".

## Dev Agent Record

### File list

- `src/types.ts` (modified) — added `AuctionStatus` union (`'upcoming' | 'live' | 'ended'`).
- `src/lib/constants.ts` (new) — `MIN_BID_INCREMENT`, `MAX_BID`, `AUCTION_DURATION`, `ENDING_SOON_THRESHOLD`, `AWAY_THRESHOLD` (values from PRODUCT.md).
- `src/lib/timestamps.ts` (new) — `normalizeTimestamps(vehicles, now)` (median-anchor, non-mutating) + `getAuctionStatus(vehicle, now)`.
- `src/lib/timestamps.test.ts` (new) — 7 tests: median-shift, empty input, non-mutation, plus the four boundary cases of `getAuctionStatus` (`now < start`, `now == start`, `now == start + duration`, `now > start + duration`).
- `src/lib/bids.ts` (new) — pure `validateBid({ vehicle, amount, now, bidderId })` returning the `BidResult` discriminated union. Order of checks: status → ceiling → floor.
- `src/lib/bids.test.ts` (new, TDD) — the 7 RED-first tests from the story spec.
- `src/lib/dataStore.ts` (new) — `DataStore` interface + `createDataStore({ now, storage, bidderId })` factory + `dataStore` singleton. Reads `data/vehicles.json`, applies median-anchor at construction, replays persisted bids from `localStorage['the-block:bids']`, layers a `Map<vehicleId, Bid[]>` over the base list, persists on every `submitBid`, fires subscribers via a `Set`. Bidder UUID stored under `localStorage['the-block:bidder-id']`.
- `src/lib/dataStore.test.ts` (new) — 7 tests covering: 200-count, live-window non-empty after normalization, source JSON not mutated, persistence + replay across a fresh factory instance, subscriber fan-out, unsubscribe, stable bidder ID across instances.
- `src/hooks/useNow.ts` (new) — module-scoped one-second clock backed by `useSyncExternalStore`. Starts the interval on the first subscriber, stops it when the last unsubscribes. Exports `__clockInternals` test seam.
- `src/hooks/useNow.test.ts` (new) — 3 tests: first-render snapshot, `vi.advanceTimersByTime` re-render, test-seam `setNow`.
- `src/hooks/useVehicles.ts` (new) — `useSyncExternalStore` wrapper around `dataStore.getVehicles()` with a cached snapshot keyed off the subscribe signal (avoids the "snapshot should be cached" warning under StrictMode).
- `src/hooks/useBids.ts` (new) — thin `submitBid` passthrough to the singleton; the seam consumers see.
- `src/pages/LandingPage.tsx` (modified) — removed the direct `data/vehicles.json` import; renders an unstyled `<ul>` of the first 20 vehicles from `useVehicles()` with the per-row status derived from `useNow() + getAuctionStatus`. The heading from Story 1.0 stays.
- `src/pages/LandingPage.test.tsx` (new) — 3 component tests: heading, list of 20, each row matches `{year} {make} {model} — (Upcoming|Live|Ended)$`.
- `src/test/setup.ts` (new) — runs `cleanup()` from `@testing-library/react` in `afterEach`; without it, component tests share DOM state across cases.
- `vite.config.ts` (modified) — registered the new `setupFiles: ['./src/test/setup.ts']`.

### Completion notes

- AC1 — `dataStore.getVehicles()` returns 200 vehicles with normalized `auction_start`. Verified by `dataStore.test.ts > returns all 200` and `> does not mutate the source JSON` (compares the first vehicle before/after store construction).
- AC2 — `getAuctionStatus(vehicle, now)` covered at all three windows including both inclusive boundaries (`now == start` and `now == start + AUCTION_DURATION` both → `'live'`).
- AC3 — Followed TDD strictly. The 7 RED tests failed against a `throw 'not implemented'` stub; after implementing `validateBid` they went green. Check order is status → ceiling → floor, so `BidError.type` is reported at the first failing condition (consistent with how the Story 1.5 `BidPanel` will pattern-match).
- AC4 — `dataStore.submitBid` persists to a single `localStorage` key (`the-block:bids`); replay verified by constructing a second store instance over the same localStorage and asserting `bid_count` increment + `current_bid` update. Subscriber fan-out and unsubscribe verified.
- AC5 — `useNow()` ticks once per second via a single module-scoped `setInterval`, started on first subscribe and stopped when the last subscriber unmounts. `vi.useFakeTimers` + `vi.advanceTimersByTime(1000)` drives the test; React 19's `act` is re-exported by `@testing-library/react`.
- AC6 — `LandingPage` renders an unstyled `<ul>` of 20 rows; the status text recomputes each render from the live `useNow()` value, so it updates on natural one-second cadence without any per-row state.
- `npm run verify` exits zero (typecheck, biome, 27 tests, build).
- Dev-server smoke check: `curl http://localhost:5173/` returned the served HTML with the expected `<title>` and `<div id="root">`. Visual confirmation in a browser is the recommended manual step.

### Change log

- 2026-05-11 — Story 1.1 implemented: data layer (constants, timestamps, dataStore), TDD `validateBid`, `useNow` / `useVehicles` / `useBids` hooks, LandingPage live list of 20, test-cleanup setup file. Status → Done.
