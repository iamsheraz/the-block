# Story 1.6 — AI condition summary enrichment

**Status:** Done
**Estimate:** 90 min
**Depends on:** Story 1.4 (for `ConditionSummary` to mount on the VDP)

## Story

Replace the placeholder text in `ConditionSummary` with real AI-generated two-sentence condition narratives, baked into the dataset at build time. The script calls Claude Haiku 4.5 once per vehicle with a system prompt that forbids price claims, repair-cost estimates, and appraisal-adjacent language — the compliance guard for a regulated dealer context.

## Files touched

- `scripts/enrich.mjs` — new build-time script using `@anthropic-ai/sdk`
- `data/vehicles.json` — gets enriched in place; committed after run
- `src/components/ConditionSummary.tsx` — update to render `vehicle.ai_summary`
- `src/types.ts` — add optional `ai_summary?: string` field to `Vehicle`
- `package.json` — `scripts.enrich`, `@anthropic-ai/sdk` dependency
- `.github/workflows/ci.yml` — add coverage check step
- `.env.example` — document `ANTHROPIC_API_KEY` for the script

## Acceptance criteria

- **AC1** `npm run enrich` reads `data/vehicles.json`, calls Claude Haiku 4.5 once per vehicle. Each call passes the vehicle's condition fields (`condition_grade`, `condition_report`, `damage_notes`, `odometer_km`, `title_status`, `year`, `make`, `model`) and writes an `ai_summary` string back into the JSON.
- **AC2** System prompt encodes five rules: (1) two sentences max, (2) describe condition story (grade, mileage context, damage themes), (3) **no dollar amounts, repair-cost estimates, market values, or appraisal-adjacent claims**, (4) no resale or wholesale price predictions, (5) mechanical concerns named generically ("reconditioning work," "diagnosis recommended") with no cost quantification.
- **AC3** Script is **idempotent**: vehicles with an existing `ai_summary` are skipped unless `--force` flag is passed. Failed API calls retry with exponential backoff up to 3 attempts.
- **AC4** Script exits non-zero if any vehicle is missing `ai_summary` after the run (coverage gate). A CI workflow step runs `node scripts/enrich.mjs --check` to verify coverage on the committed dataset.
- **AC5** Output validation: the script rejects and retries any generated summary containing a `$` character, the words "estimated cost", "market value", "appraised at", or numeric repair amounts. After 3 retries, the vehicle is logged and the script exits non-zero.
- **AC6** `ConditionSummary` component reads `vehicle.ai_summary` and renders it inside the `BidPanel` (slot defined in Story 1.4). The label is **honest about its source**:
  - When `ai_summary` is present (Claude-generated): heading `💬 AI summary` with a chat-bubble icon.
  - When `ai_summary` is missing (graceful degradation): heading `📋 Condition snapshot` with a clipboard icon; body renders a templated sentence derived from `condition_report` and `damage_notes` count.
  - Templated text never appears under the "AI summary" label. The chat-bubble icon is reserved for actual AI synthesis.
- **AC7** The Anthropic API key is read from `process.env.ANTHROPIC_API_KEY` in the script only. Never imported by any file in `src/`.

## System prompt for `scripts/enrich.mjs`

```
You write two-sentence condition summaries for used vehicles in a wholesale
auction listing. Given the vehicle's condition fields, write a summary that
helps a professional buyer assess risk in under 30 seconds.

Rules:
1. Exactly two sentences.
2. Describe condition (grade context, mileage context, damage themes).
3. Do NOT include dollar amounts, repair-cost estimates, market values,
   appraisal claims, or any numeric figures other than the grade or mileage.
4. Do NOT predict resale or wholesale price.
5. Mechanical concerns must be named generically — "reconditioning work,"
   "diagnosis recommended" — never quantified by cost.

Output only the two-sentence summary. No preamble, no quotes, no labels.
```

## Tests

- Smoke run against a fixture of 3 representative vehicles (clean, rebuilt, salvage).
- Output validator: assert no `$`, no "estimated cost", no "appraised", no four-digit numbers other than mileage and year.
- `ConditionSummary renders ai_summary when present`
- `ConditionSummary renders templated fallback when ai_summary is missing`
- `ConditionSummary uses "AI summary" label and chat-bubble icon when ai_summary is present`
- `ConditionSummary uses "Condition snapshot" label and clipboard icon when ai_summary is missing`

## How to verify (UI)

1. Set `ANTHROPIC_API_KEY` in your local `.env` file.
2. `npm run enrich` — script runs through 200 vehicles (~2-3 minutes with rate limits). Console logs each completion.
3. Re-run `npm run enrich` — script reports "all vehicles already enriched, skipping" and exits zero (idempotency check).
4. Open `data/vehicles.json` — every vehicle has an `ai_summary` field with a two-sentence description.
5. Grep the file for `$` — only price fields contain `$`, never inside `ai_summary` strings.
6. `npm run dev` — visit any `/vehicle/:id`.
7. The bid panel now shows real AI summary text replacing the Story 1.4 placeholder.
8. Try a vehicle whose `ai_summary` you manually delete and reload — the templated fallback renders cleanly.

## Commit

```
feat: build-time AI condition summaries with compliance-safe prompt
```

## Watch for / Notes

- This story **depends on Story 1.4** because `ConditionSummary` lives inside the VDP's bid panel.
- **Label honesty.** The chat-bubble icon and "AI summary" wording appear only when `ai_summary` is actually populated from Claude. The templated fallback is honest about being a templated summary — different icon, different label. This protects the trust signal across the platform: a buyer who sees "AI summary" knows that label means actual AI synthesis. Sheraz runs `npm run enrich` once with his Anthropic API key, commits the enriched JSON, and ships — the fallback only renders if enrichment fails on a specific vehicle (which the coverage gate prevents).
- The compliance guard (no price figures) is enforced **twice**: in the system prompt as instruction, and in the script as output validation with retry. Defense in depth.
- The Anthropic API key never leaves the build environment. The committed JSON contains the static summaries; the client bundle has no API code.
- Re-running enrichment is cheap (skips already-done vehicles). Use `--force` only when you want to regenerate everything (e.g., after a prompt change).
- For local dev without an API key, the templated fallback in `ConditionSummary` keeps the UI functional — the AI summary is an enhancement, not a blocker.

## Story complete when

- Every AC above is true on `main`.
- All 200 vehicles in `data/vehicles.json` have a compliance-safe `ai_summary`.
- CI coverage check passes.
- `npm run verify` exits zero in CI.
- Status above updated to "Done".

## Dev Agent Record

### Implementation plan

1. Add optional `ai_summary?: string` to the `Vehicle` type so consumers can read it without a wide-cast.
2. Build `scripts/enrich.mjs` around the Anthropic Node SDK (`@anthropic-ai/sdk`):
   - Reads `data/vehicles.json`, skips vehicles already enriched (unless `--force`).
   - Calls `claude-haiku-4-5-20251001` with the AC2 system prompt and a structured user message of the vehicle's condition fields.
   - Retries up to 3 times with exponential backoff on API errors **or** compliance-validator rejections.
   - Persists `data/vehicles.json` after each successful vehicle so a crash mid-run doesn't lose work.
   - `--check` mode reads the dataset and exits non-zero on any missing summary; CI invokes it.
3. Extract a pure `validateSummary(text, vehicle)` and unit-test it against the AC5 rules + a 3-fixture smoke set (clean / rebuilt / salvage).
4. Rewrite `ConditionSummary` to accept the vehicle and switch between the AI label (chat-bubble icon, "💬 AI summary") and the templated fallback (clipboard icon, "📋 Condition snapshot") based on `ai_summary` presence. Honest label switching is the trust signal.
5. Wire the `npm run enrich` script (uses `node --env-file-if-exists=.env`), `.env.example`, and a CI coverage step.

### Completion notes

- All 200 vehicles enriched on first run + second pass. `node scripts/enrich.mjs --check` passes; `npm run verify` exits zero (32 test files, 251/251 tests, typecheck, lint, build).
- AC5 validator is defense-in-depth: rejects `$`, "estimated cost", "market value", "appraisal"/"appraised at", "resale value", "wholesale price", any 4-digit number that isn't the vehicle's year, part of the odometer, or a digit token in the model name (e.g. "1500" for a Ram 1500), and any text with more than two sentences. Three retries before the script logs a failure and exits non-zero.
- First enrichment run produced 175/200 successes and 25 retry-exhausted failures, all of them Ram 1500 / GMC Sierra 1500 / Chevrolet Silverado 1500 — Claude legitimately wrote "1500" as part of the model name, but the original validator flagged it as a disallowed 4-digit number. Fixed the validator to also allow digit tokens that appear in `vehicle.model`. Added a regression test (`scripts/enrich.test.mjs`). Re-ran enrichment; all 25 trucks succeeded on first attempt.
- Grepped enriched JSON for `$` — zero hits inside any `ai_summary`. AC5 compliance gate holds end-to-end.
- AC7 holds: `process.env.ANTHROPIC_API_KEY` is read only inside `scripts/enrich.mjs`. Nothing under `src/` touches it. The SDK is a `devDependency` and never bundles into the client.
- `ConditionSummary` test file was rewritten for the new signature; the BidPanel "AI summary panel" test was split into two — one for the AI-present path, one for the templated-fallback path.
- Shebang on `scripts/enrich.mjs` was dropped because vitest's transformer choked on it when the test file imported the script. The script is always invoked via `node`, so the shebang was decorative anyway.

### File list

- `src/types.ts` — added optional `ai_summary?: string` to `Vehicle`.
- `src/components/ConditionSummary.tsx` — new `vehicle` prop; AI vs templated fallback with honest labels and icons.
- `src/components/ConditionSummary.test.tsx` — rewrote for new signature; covers all 4 spec cases plus whitespace edge case.
- `src/components/BidPanel.tsx` — passes `vehicle` into `ConditionSummary`.
- `src/components/BidPanel.test.tsx` — split AI summary panel test into present/missing cases.
- `scripts/enrich.mjs` — new build-time enrichment script (system prompt, retry/backoff, compliance validator, `--force` and `--check` flags, per-vehicle persistence).
- `scripts/enrich.test.mjs` — new unit tests for `validateSummary` across clean/rebuilt/salvage fixtures and forbidden phrases; includes the Ram 1500 / Silverado 2500 model-token regression test.
- `package.json` — added `@anthropic-ai/sdk` devDependency, `enrich` and `enrich:check` npm scripts.
- `package-lock.json` — generated.
- `.env.example` — documents `ANTHROPIC_API_KEY` for the script.
- `.github/workflows/ci.yml` — new "AI summary coverage gate" step.
- `data/vehicles.json` — enriched in place: all 200 vehicles now carry an `ai_summary`.

### Change log

- 2026-05-12 — Implementation complete; enrichment run against full 200-vehicle dataset; validator hardened to allow model-name digit tokens after first-run truck failures. `npm run verify` green, coverage gate green, status flipped to **Done**.
